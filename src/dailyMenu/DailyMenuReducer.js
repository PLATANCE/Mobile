'use strict';
import { ADD_CART, CHANGE_DATE } from './DailyMenuActions'

export default function dailyMenus(state = {}, action)  {
    switch (action.type) {
        default: 
        	return state;
    }
}
